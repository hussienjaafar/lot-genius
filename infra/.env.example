# Keepa API Configuration
KEEPA_KEY=

# Database Configuration
POSTGRES_URL=postgresql://username:password@localhost:5432/lotgenius  # pragma: allowlist secret
REDIS_URL=redis://localhost:6379/0

# Scrapers
ENABLE_SCRAPERS=false

# External Comps Scrapers (DISABLED BY DEFAULT)
# IMPORTANT: These scrapers must comply with ToS of respective sites
# Set SCRAPER_TOS_ACK=true only after reviewing and accepting terms
ENABLE_EBAY_SCRAPER=false
ENABLE_FB_SCRAPER=false
SCRAPER_TOS_ACK=false

# External Comps Configuration
EXTERNAL_COMPS_PRIOR_WEIGHT=0.1
EXTERNAL_COMPS_LOOKBACK_DAYS=90
EXTERNAL_COMPS_MAX_RESULTS=10

# Decision policy (minimums — not hard-coded targets)
MIN_ROI_TARGET=1.25
SELLTHROUGH_HORIZON_DAYS=60
RISK_THRESHOLD=0.80
CASHFLOOR=0

# Additional Configuration
CLEARANCE_VALUE_AT_HORIZON=0.50
RECENCY_DECAY_LAMBDA=0.03

# Source Priors
SOURCE_PRIOR_AMAZON=0.7
SOURCE_PRIOR_OTHERS=0.3

# Return Rates by Category
RETURNS_ELECTRONICS_MIN=0.08
RETURNS_ELECTRONICS_MAX=0.12
RETURNS_APPAREL_MIN=0.03
RETURNS_APPAREL_MAX=0.06
RETURNS_SALVAGE_MIN=0.25
RETURNS_SALVAGE_MAX=0.40

# Marketplace Fees
MARKETPLACE_FEE_PERCENT=0.03
MARKETPLACE_FEE_FIXED=0.40

# Header Coverage Validation
HEADER_COVERAGE_MIN=0.70
